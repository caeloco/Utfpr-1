#include <stdio.h>



int main () {
    int largura, comprimento;

    scanf("%i %i", &largura, &comprimento);

    printf("a area do terreno eh: %i", comprimento * largura);

    return 0;
}